---
export interface Props {
    href: string;
    label: string;
}

const { href, label } = Astro.props as Props;
---

<a href={href} class="flex">
    <span
        class="transition-colors sm:text-lg text-base font-semibold group-hover:text-neutral-600 dark:group-hover:text-neutral-400 text-black dark:text-white hover:hover:hover:text-black hover:hover:hover:dark:text-white"
    >
        {label}
    </span>
</a>
